# 1. Setup Arduino IDE for ROS:
#    a) sudo apt-get install ros-noetic-rosserial-arduino
#    b) cd ~/Arduino/libraries  (or wherever your sketchbook libraries are)
#    c) rm -rf ros_lib
#    d) rosrun rosserial_arduino make_libraries.py .

# 2. Open Arduino IDE, paste the code, and upload to your board.

# --- Running the Node ---
# Open 3 separate terminals.

# Terminal 1: Start ROS Master
roscore

# Terminal 2: Start the rosserial connection node.
# Replace /dev/ttyACM0 with your Arduino's serial port.
rosrun rosserial_python serial_node.py /dev/ttyACM0

# Terminal 3: Echo the 'chatter' topic to see the messages from the Arduino.
rostopic echo chatter
